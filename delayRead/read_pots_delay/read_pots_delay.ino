



float  mult = 1.4252

void loop() {
  float fb =(float)analogRead(A1) / 2000.0f;
  float fb =(float)analogRead(A2) * mult
  float fb =(float)analogRead(A2) * mult
  float fb =(float)analogRead(A2) * mult
  float fb =(float)analogRead(A2) * mult

  delay1.delay(0, dly0 * mult);
  delay1.delay(1, dly0 * mult);
  delay1.delay(2, dly0 * mult);
  delay1.delay(3, dly0 * mult);
  if (fb < 0.04)  fb =0;
 delay (50);
}

